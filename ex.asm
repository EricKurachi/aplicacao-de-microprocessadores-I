	ORG 0
DMUX:	MOV 	DPTR, #1000H
	MOV 	R0, #01
CONT:	ACALL 	APAGA
	MOVX    A, @DPTR
	MOV     P1, A
	MOV 	P2, R0
	MOV 	A, R0
	RL	A
	MOV     R0, A
	MOV     P1, A
	MOV 	P2, R0
	MOV 	A, R0
	RL	A
	MOV     R0, A
	CJNE	R0, #00000100B, FIRST
	MOV 	DPTR, #2000H
	SJMP	CONT
FIRST:	CJNE	R0, #00010000B, SECOND
	MOV 	DPTR, #3000H
	SJMP	CONT
SECOND:	CJNE	R0, #10000000B, THIRD
	MOV 	DPTR, #4000H
	SJMP	CONT
THIRD:	MOV 	DPTR, #1000H
	SJMP	CONT
APAGA:	MOV	P1, #00
	MOV	P2, #00
	RET
	END

